{ Calculate the factorial of a postive integer }
BEGIN
  READ factorial_of

  counter = factorial_of
  accumulator = 1

  LOOP
    IFZ counter
      BREAK
    END

    accumulator = accumulator * counter
    counter = counter - 1
  END

  PRINT accumulator
END
